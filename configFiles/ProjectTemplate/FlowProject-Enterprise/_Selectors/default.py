# FileName: default.py
"""
Project selector definitions.

Store reusable window, UIA, HTML, and image selectors in this module.

For static type checking, import the required selector types from
`liberrpa.Modules` and annotate each selector with the most specific type,
such as `SelectorWindow`, `SelectorUia`, `SelectorHtml`, or `SelectorImage`.

Small projects can keep selectors in this file. Larger projects should split
them into focused modules under `_Selectors`.
"""
